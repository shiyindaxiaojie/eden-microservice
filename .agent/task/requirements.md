1、服务注册增加命名空间隔离，单独加个菜单页面【命名空间】，新增命名空间维护，单独存储到 namespace.json。命名空间对于客户端集成只是可选项，增加命名空间对于 pkg 客户端集成不要有影响，避免我替换 consul 代码出问题，只是作为隐藏可选项。
2、服务列表界面需要优化，查询条件需要支持 服务名称、IP、健康状态，查询列表支持网格和列表视图切换，交互上重新设计下、另外，针对某个实例 【注销】改为【下线】和【上线】，点击下线后，需要通知被下线客户端，客户端不在继续发送心跳等操作，点击上线，需要通知被下线客户端，客户端立刻拉取最新的订阅信息，并继续发送心跳等操作。查询列表字段需要追加订阅者个数统计，点击订阅者个数可以查看订阅者列表
3、服务之间存在服务提供方和服务消费方的关系，在页面需要增加一个服务依赖关系图，显示当前命名空间所有的服务之间的依赖关系
4、目前客户端优先依赖注册中心，但是注册中心可能会出现完全宕机，需要增加一个本地缓存机制，客户端启动时先从本地缓存加载服务列表，然后尝试连接注册中心，如果连接失败，继续使用本地缓存，并定期更新本地缓存
