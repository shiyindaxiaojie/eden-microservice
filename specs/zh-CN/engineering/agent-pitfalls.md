# 智能体易错点登记

仅登记已由用户纠正、测试或代码评审确认，且可在其他任务复用的规则。任务开始前按领域和关键词查询；不得记录探索失败、瞬时网络或本机环境问题。

## 条目模板

### <ID>: <简短标题>

- 状态：confirmed
- 日期：YYYY-MM-DD
- 症状：
- 根因：
- 防护规则：
- 适用范围：
- 验证：

### DIR-001: 目录清理必须覆盖整个仓库

- 状态：confirmed
- 日期：2026-07-28
- 症状：只清理任务中点名的配置目录，遗漏了根目录下无源码的 `web` 副本和其他废弃目录。
- 根因：没有先按顶层目录清点 Git 跟踪状态、构建入口和引用关系。
- 防护规则：处理“无用目录清理”时，必须审计全部顶层目录以及 tracked、untracked、ignored 内容，并核对 `go.work`、Docker、CI 和文档引用；运行数据目录不得按无引用代码处理。
- 适用范围：仓库结构调整、模块迁移、重复目录清理。
- 验证：检查顶层目录清单、`git status --short`、旧路径全文引用和结构契约测试。
